CODE-FIRST APPROACH


1) dotnet core provide certain tool to create class... into table
2) Migration is a tool to apply in code first approach
3) We have to install the package "dotnet tool install --global dotnet-ef"

6/07/2024

4) We have completed code first approach with the operations.
5) 

Now while converting the existing application to DB FIRST APPROACH
// Remark: 
1. for enterprise application when we have larger number of coulmns and more tables then DB FIRST is the best approach.